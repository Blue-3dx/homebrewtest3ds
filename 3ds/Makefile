
# devkitPro / 3DS Makefile (builds a .3dsx)
TARGET := GDWave3DS
BUILD	:= build
SOURCES := main.c
# Use romfs directory at project root
# Add include path if needed
INCLUDES :=
LIBS :=
# Use the samples Makefile include (standard for devkitPro)
include $(DEVKITPRO)/3ds/samples/Makefile
